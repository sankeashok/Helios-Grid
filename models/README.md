# Helios-Grid Models Directory
# This directory will contain trained ML models for energy consumption prediction

# For local testing, the application uses a rule-based model
# In production, this would contain:
# - model.pkl (trained model)
# - scaler.pkl (feature scaler)
# - feature_names.pkl (feature names)
# - model_metadata.json (model information)

# The local test application will create a simple rule-based model
# that doesn't require these files